local:
    root: /var/www/d8cards-site/docroot
    uri: 'http://local.d8cards-site.com'
    host: local.d8cards-site.com
    user: vagrant
    ssh: { options: '-o PasswordAuthentication=no -i $HOME/.vagrant.d/insecure_private_key' }
